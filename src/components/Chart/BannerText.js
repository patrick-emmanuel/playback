import P from '../P';

const BannerText = P.extend`
  display: block;
  position: absolute;
  font-size: 2em;
  font-weight: 700;
  letter-spacing: 2px;
  bottom: 90px;
  right: 30px;
`;

export default BannerText;
